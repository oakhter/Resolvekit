# ResolveKit Experiments

- `runs/` stores timestamped offline run outputs.
- `reports/` stores human-readable summaries and latest report aliases.
- `decisions/` stores ship/no-ship notes for variants.

Stage 2 runner:

```bash
.venv/bin/python scripts/run_ab_stage2_eval.py
```
